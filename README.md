# Generated Frontend

This frontend was generated by ConceptualAI.

## Run locally

1. Create an environment file:
   - Copy `.env.template` to `.env`
2. Install dependencies:
   - `npm install`
3. Start the dev server:
   - `npm run dev`

Open the local URL printed in the terminal.

## Notes

- This frontend expects the generated backend to be running.
- For local runs, default frontend `.env` values are usually correct.
- Update `.env` only if your backend URL/domain is different.
